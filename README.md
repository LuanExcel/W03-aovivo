# CRIAR PYTHON VERSION
> pyenv local 3.11.5



# POETRY
>> PARA CRIAR O pyproject.Toml
    >> poetry init

>> Ativando o ambiente virtual
    >> poetry env activate


ignr -p python > .gitignore    


# site toptal
>> para pegar um modelo de .gitignore

# ignr cli python
>> outra opção .gitignore

# git rebase -i --root
> para editar meus comits
git commit --amend

# mkdocs
pip install mkdocs
mkdocs new .
mkdocs serve
mkdocs deploy


git add mkdocs.yml README.md
git commit -m "docs(mkdocs e README): adicionar documentacao"

# Para instalar esse projeto

```bash
git clone git@github.com:lvgalvao/W03-aovivo.git
cd W03-aovivo
```

```bash
pyenv install 3.11.5
pyenv local 3.11.5
```

```poetry
poetry shell
poetry env use 3.11.5
poetry install
```

para testar digite:
```bash
duckdb
```
